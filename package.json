{
  "name": "jack-of-all-decorators",
  "version": "1.0.0-beta1",
  "description": "Annotation library for TypeScript.",
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "scripts": {
    "compile-lib": "rm -r lib/* && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "mocha": "^2.5.3",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  },
  "author": "Patrick Martin",
  "license": "ISC",
  "dependencies": {
    "dateformat": "^1.0.12",
    "lodash": "^4.15.0"
  }
}
